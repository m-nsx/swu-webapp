import React from 'react';

const SearchEngine = () => {
    return (
        <div>
            <h2>Search Engine</h2>
            <p>Use this page to search for cards in the database.</p>
        </div>
    );
};

export default SearchEngine;